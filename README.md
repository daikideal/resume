# resume

## 株式会社ビットキー プラットフォーム開発部 (2023/10 ~ 現在)

### 不正検知つきログイン機能 (2026/06 ~ 進行中)

#### 概要

自社の認証認可基盤を利用するコンシューマ向けプロダクトで発生した不正ログイン疑い事案を受け、未知端末からのログインを検知・通知する機能の設計・実装を推進している。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 課題整理・設計ドキュメント作成
- ログインAPI・端末列挙APIの実装
- 通知テンプレート・E2Eテストを含むクロスリポジトリでのリリース調整

#### 使用技術

- Go
- gRPC
- PostgreSQL
- Redis
- Google Cloud
  - Pub/Sub

#### 課題

自社の認証認可基盤(bkp)を利用するコンシューマ向けプロダクトにおいて、不正ログインの可能性がある事案が発生した。当時の認証フローでは、不正なアクセスがあってもユーザーが後から気づく手段がなく、多要素認証などの追加の安全対策も存在しなかった。

#### 取り組み

- ログインした端末の情報を記録する`user_terminals`テーブルを新設し、未知の端末からのログインを検知する`POST /users/signin`(不正検知つきログインAPI)を実装。未知端末を検知した場合はPub/Sub経由で通知サービスに連携し、メール/SMSでユーザーへ通知
- ユーザー自身がログイン済みの端末に心当たりがあるか確認できるよう、`GET /users/user_terminals`(端末列挙API)を実装
- 通知メッセージテンプレート(別リポジトリ)・E2Eシナリオテスト(別リポジトリ)を含めた3リポジトリにまたがる変更が全て揃うまでマージしない方針を取り、機能を段階的かつ安全にリリース

#### 工夫した点

実装の過程で、端末列挙APIのレスポンスに含まれる`terminal.id`がログインCookieの値としてそのまま信頼されていることに気づいた。この`id`さえ入手できれば他人の端末になりすまして不正検知を回避できてしまう脆弱性であり、内部識別用の`id`とCookieに使う値を分離し、推測不可能な`token`カラムを別途追加することで自ら発見・是正した。
また、チームの振り返りで挙がった「設計を実装前にレビューしたい」というTRY項目を受け、設計ドキュメントをリポジトリ内にMarkdownで管理し、GitHub Copilotによる設計レビューを受けられる体制の構築にも着手している。

#### 成果

ログインAPI・端末列挙API・未知端末検知時の通知機能をリリースした(2026年7月時点)。設計ドキュメントの整備とAIによる設計レビュー体制の構築は現在進行中である。

### コンテナ/Podのnon-root化 (2026/05 ~ 2026/06)

#### 概要

セキュリティ強化の一環として、認証認可基盤(bkp)の各コンテナ・Podをnon-root実行に変更した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- root実行のリスク調査・整理
- 各サービスのDockerfile/Kubernetes Manifestの修正
- 展開・動作確認

#### 使用技術

- Docker
- Kubernetes (GKE)

#### 課題

以前、コンテナユーザーが権限昇格できないようにする対策を実施していたが、そもそもコンテナ自体がroot権限で実行されていては効果が薄い。コンテナがrootで実行されている場合、攻撃者にコンテナへ侵入された際にコンテナエスケープされ、GKEクラスタ全体の管理者権限や他のGoogle Cloudリソースまで侵害される恐れがあった。

#### 取り組み

- GKE Sandbox・User Namespacesなどコンテナをrootで実行しても問題ない構成にする代替案も検討したうえで、bkpのサービス実行にはroot権限が必要な理由がないと判断し、全サービスのnon-root化を選択
- 本番で稼働しているDockerfileを優先度(サーバー>Job)順に、appgate・delivery・frs・idgate・idcontroller・dbcommitter・well-known・frs-migrate・membercleaner・newtenantの各サービスにnon-rootユーザーを追加
- Kubernetes Manifestに`runAsNonRoot: true`を追加し、コンテナランタイム側でもroot実行を制約

#### 工夫した点

`runAsNonRoot: true`を追加してデプロイしたところ、「Dockerfileの`USER`がUIDではなくユーザー名で指定されているとnon-rootであることを検証できない」というエラーが発生することを発見し、`USER`ディレクティブを数値UID指定に修正した。
また、appgate内で稼働するnginxコンテナがroot権限で実行されていた問題についても、nginx-unprivilegedイメージへの切り替えと設定ファイルの調整、`securityContext`への`runAsUser`/`runAsGroup`追加により対応した。

#### 成果

本番で稼働する主要サービス(appgate, delivery, frs, idgate, idcontroller, dbcommitter, well-known, frs-migrate, membercleaner, newtenant)のnon-root化を完了し、コンテナエスケープ時の被害範囲を最小化した。

### CI/CDセキュリティ強化 (2025/06 ~ 2026/06)

#### 概要

CI/CDパイプラインにおけるサプライチェーンリスクの低減と、DBマイグレーション・Dockerfileの安全性チェックの仕組みづくりを推進した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 既存ツール(Dangerbot)の代替設計・実装
- サードパーティGitHub Actions依存の棚卸し・削減
- Dockerfileリンター(hadolint)の導入・横展開

#### 使用技術

- GitHub Actions
- Bash
- Go
- hadolint

#### 課題

Rubyベースかつサードパーティ製ツールであるDangerbotに、DBマイグレーションの危険な操作(DROP/RENAME)の検知など一部の必要な機能のためだけに依存していた。また、`tj-actions/changed-files`のように実際にサプライチェーン攻撃が発生したこともあるサードパーティGitHub Actionsが、複数のリポジトリで利用されたままになっていた。加えて、リポジトリごとにDockerfileの品質にばらつきがあった。

#### 取り組み

- Dangerbotを削除し、DROP/RENAME検知とpackageレベル外での`errors.New()`使用の検知を、自前のbashスクリプト(`check_unsafe_operation`)に置き換え
- `tj-actions/changed-files`、`shmokmt/actions-setup-tfcmt`、`quarylabs/install-sqruff-ci-action-sqruff`など、複数のサードパーティActionsを公式配布バイナリや自前のインストール処理に置き換え
- Dockerfileの静的解析ツールhadolintをbkpに導入し、idgate・faceme-base-imageへ横展開
- 他メンバーが導入したActionsセキュリティスキャナ(zizmor)の運用保守(公式Actionへの移行、バージョンアップ対応)を担当

#### 工夫した点

サードパーティActionsの削減にあたっては、コードサーチでorg全体の利用箇所を棚卸ししたうえで対応した。tfcmtの置き換えでは、公式リリースバイナリを取得するだけでなく`gh attestation verify`によるSLSA provenance検証まで組み込み、サプライチェーンの信頼性を担保した。
`check_unsafe_operation`の導入時には、`actions/checkout`のデフォルト挙動(マージコミットをチェックアウトしてしまう)に起因してPRブランチとの差分検知が正しく動作しない不具合が発覚し、複数のPRにわたって原因を切り分けながら確実に動作する状態まで作り込んだ。

#### 成果

Dangerbotおよび複数のサードパーティActionsへの依存を排除し、自組織で保守可能なセキュリティチェック体制を構築した。hadolintは3リポジトリへ展開した。

### Cloud SQL IAM認証移行 (2025/06 ~ 2026/05)

#### 概要

Cloud SQL for PostgreSQLのパスワード認証を廃止し、GitHub Actions・アプリケーションサービスアカウントをIAM認証で接続できるよう移行した。チーム内の施策ではあったが、移行方針の設計から実装まで自ら担当した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 移行方針の設計・Terraformでのリソース実装(個人で担当)
- 権限(GRANT)管理の仕組み構築(個人で担当)

#### 使用技術

- Terraform
- Google Cloud
  - Cloud SQL for PostgreSQL
  - IAM
  - Workload Identity Federation
- GitHub Actions

#### 課題

GitHub ActionsからDBマイグレーションを実行できるようにする対応を進める中で、そのために使用していたWorkload IdentityがSecret Manager上の任意のシークレットバージョンを取得できてしまう、権限が過剰な状態になっていることが判明した。パスワードベースの認証がSecret Manager上のパスワード管理に依存している以上、同様の権限過多のリスクは残り続けるため、パスワード認証自体を廃止し、Cloud SQLのIAM認証へ移行することを決定した。

#### 取り組み

- GitHub ActionsおよびDB接続が必要な各サービスのサービスアカウントが、Cloud SQLにIAM認証で接続できるようTerraformでリソース・設定を追加
- IAM認証で接続できるようになったサービスアカウントに対して、SQLを実行するためのGRANTをTerraformで管理する仕組みを追加。当初は自前のスクリプトで実装したが、検証・保守コストの高さから、サードパーティのTerraformモジュール(`cyrilgdn/postgresql`)を使う構成に置き換え
- 重複していたTerraformモジュールを1つに統合し、GRANT管理の仕組みを一本化

#### 工夫した点

GRANT管理用のTerraformモジュール選定にあたっては、`cyrilgdn/postgresql`と`GoogleCloudPlatform/sql-db/google//modules/postgresql`を比較検討し、Terraform Registry上のダウンロード数など客観的な指標をもとに`cyrilgdn/postgresql`を採用した。
また、GRANT管理をアプリケーションリポジトリ(bkp)側で行うかインフラリポジトリ(terraform)側で行うかについてもチーム内で議論になったが、Terraformでのリソース管理という点での一貫性やファイル編集のサイクルを踏まえ、terraformリポジトリに寄せる判断を行った。

#### 成果

GitHub Actions・アプリケーションサービスアカウントの双方について、パスワードを使わないIAM認証への移行を実現し、DB認証情報の漏洩リスクを低減した。

### SAML/SCIM連携作業の自動化 (2025/04 ~ 2025/05)

#### 概要

従来、開発者が手作業で行なっていたSCIM/SAML連携作業をAPI経由で完結できる機能を設計・実装した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 業務シーケンスの整理・図の作成
- API設計の叩き台作成

#### 使用技術

- Go
- Google Cloud
  - Identity Platform
  - Cloud Firestore
- HTML
- JavaScript

#### 課題

前提として、自社プロダクトをSP(Service Provider)とした SAML SSO・SCIMプロビジョニング の機能はすでに認証認可基盤チームにより開発・運用されていた。運用される中で2つの課題が出てきた。

1. 開発当初は連携作業依頼はそれほど件数も多くなく特に困っていなかったが、営業の方針としてエンタープライズ向けの導入が増え、連携作業依頼が増加してきていた
2. また、対応しているIdPの一つであるOkta様より、OIN(Okta Integration Network)に弊社プロダクトを載せないかという旨の提案があった。連携手順をOINに掲載することになるが、手作業のままだと「弊社窓口まで連絡」という手順を載せることになり、開発者がボトルネックになってスケールしない懸念があった

#### 取り組み

- SAML/SCIM連携の導入と運用をお客様の手で完結できるようにするための機能をリリース
  - Identity Platform Admin SDKを使用したSAMLプロバイダー作成API
  - SAML証明書更新API
  - SCIMシークレットの発行API
  - SCIMシークレット更新API
- API経由での連携作業をプロダクト側の開発者が体験できるよう、HTMLとJavaScriptで動くデモサイトを社内検証環境にリリース

#### 工夫した点

プロジェクト始動当初はSAML/SCIM連携の**導入**だけが視野に入っていて、"SAMLプロバイダー作成API"と"SCIMシークレットの発行API"だけだったが、業務シーケンスの整理にあたってSAML/SCIM連携についてのユースケースを洗い出していると、過去に「証明書の期限が切れていたのでSSOに失敗した」という問い合わせがお客様より寄せられていたことを発見した。
その問い合わせは開発者が手作業でお客様のIdPのメタデータXMLより証明書の値を取得してIdentity Platformに入力することで対応したが、証明書の更新もAPI経由でできたほうがよりリードタイムを減らし、お客様に自走していただけるプロダクトになると考え、このタイミングでAPIとして実装することにした。

#### 成果

認証認可基盤側の機能としては、SAML/SCIM連携を手作業なくAPI経由のみで完結させられるようになった。
2025年11月にプロダクト側でSAML/SCIM連携機能の実装が開始したため、プロダクトを通した市場投入の時期は未定。

### Cloud SQLのサービス無停止アップグレード (2025/04 ~ 2025/05)

#### 概要

Cloud SQL for PostgreSQLをサービス無停止でアップグレード

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- アップグレード方法の選定
- アップグレード作業

#### 使用技術

- Cloud SQL Enterpriseエディション
- PostgreSQL

#### 課題

家庭向け・オフィス向けスマートロックサービス共通の認証認可基盤のデータベースのアップグレードであるため、メンテナンス期間を設けることができない。

その他の前提:

- プライマリインスタンス×1、レプリカインスタンス×3
- アプリケーションはマイクロサービスで、データベースは3つに分かれている
- アプリケーションはGKEクラスタで稼働
- コネクションプールは使用していない

#### 取り組み

Postgres組み込みの論理レプリケーション機能を使用して旧DBから新DBへデータを移行し、アプリケーションからの接続を切り替える作業を実施

#### 工夫した点

3つの方法を検討した。

1. インプレースアップグレード
2. Google CloudのCloud DMS
3. Postgres組み込みの論理レプリケーション

1.は、プライマリをアップグレードするためにレプリカを停止する必要があり、プライマリも20分~1時間停止する。レプリカからアップグレードしてフェイルオーバーも考えたが、Postgresはバージョンが異なるインスタンス同士のレプリケーションをサポートしない。どうしてもサービス停止期間が発生するため不採用。

2.は、データベースごとに移行元インスタンスをプライマリ・移行先インスタンスをレプリカとして"接続プロファイル"を作成することでデータを同期することになる。しかしDMSの仕様上、移行元インスタンスから移行先インスタンスに対して同時に複数の"接続プロファイル"を作成することはできず、かつ移行先インスタンスのプロモート時にはデータ不整合を防ぐためにアプリケーションを停止することがGoogle Cloud公式より推奨されている。よって、作業期間が長引くうえにサービス停止期間が数回発生することになるため不採用。

3.は、自前でコマンドを実行して移行元・移行先インスタンス間のレプリケーションを組む手間がかかるものの、複数のデータベースを同時にデータ同期することができるので最終的な作業期間は長くはならない。さらに、レプリケーションを組んだあとはアプリケーション側のデータベース接続情報を移行元から移行先へと修正してローリングアップデートすればデータ不整合などは発生しない見込みが立ったため、採用。

検証環境で検証し、データ不整合やその他のトラブルも発生しないことを確認したので本番環境で決行したところ、503エラーが一時的に数件発生したのみで、問題なくアップグレードが完了した。

#### 成果

Cloud SQL for PostgreSQLを、PostgreSQL 14からPostgreSQL 16へメンテナンス期間等を設けずにアップグレードすることができた。

学びとして以下のような知見を得た。

- PostgreSQLは、無停止でのメジャーバージョンアップに役立つ論理レプリケーションという機能が組み込みで存在する
- マイクロサービスごとにデータベースを分ける場合、特定のサービスのアップグレードが他のサービスに影響を与えないようにインスタンスごと分ける方がよい

### 顔認証用ライブラリを後方互換性を維持してアップデート (2024/09 ~ 2024/11)

#### 概要

認証のために顔画像から顔特徴量を抽出するライブラリ(FaceMe SDK)のアップデート計画をVPoPや他チームと調整し、自ら設計・実装も行った。

#### 担当

チームメンバー:

- エンジニア3名

役割:

- VPoPや他チーム、SDKベンダーとのコミュニケーション
- SDKの動作検証
- バックエンドの設計・実装

#### 使用技術

- Go
- Google Cloud
  - Pub/Sub
- C++

#### 課題

前提として、FaceMe SDKによる顔特徴量抽出機能は従来より市場投入されていた。
認証認可基盤側でマスターとなる顔画像と顔特徴量を登録し、クライアントとなるプロダクト側では端末に顔をかざすとその場で特徴量を抽出し、認証認可基盤で持つ特徴量とアプリが抽出した特徴量の類似度で認証を行う。
この顔特徴量には"モデル"という概念が存在し、新しいモデルほど精度の高い認証を実現できる。

SDKのベンダーよりSDKの最新バージョンがリリースされた旨の連絡をいただいた。アップデート内容としては、最新の顔特徴量モデルが利用できるようになったほか、顔特徴量の抽出速度や特徴量同士の比較速度が上がるとのことだった。
プロダクト側のロードマップとして、最新のSDKにバージョンアップして顔認証のUXを向上させるという計画があったが、認証認可基盤側が対応しなければプロダクト側でも最新モデルが使用できないため、プロジェクトとしてSDKのアップグレードを行うことを決定した。

最新バージョンのSDKを組み込んだコンテナをビルドして動作検証を行ったところ、市場で現在も使用している古いモデルが廃止されていることが判明。
古いSDKを搭載したアプリを利用している顧客は多く、その端末はビルのエレベーターホールやオフィスの入り口に設置されているケースもあり、顧客との調整が必要で、すぐの完全移行ができない。愚直に認証認可基盤側でSDKのアップグレードを行ってリリースしてしまうと、顔情報の登録を行えないという事象が発生する恐れがあった。

#### 取り組み

- 新旧SDKを同時に稼働させる設計を行い、最新モデルへ移行するスケジュールも含めてVPoPやプロダクト開発チームと合意
- C++で実装されたSDK、およびSDKとの繋ぎ込み機能のデバッグ
- Goを使用したバックエンド開発

#### 工夫した点

認証認可基盤はマイクロサービスで、大きくは以下４つに分かれている。

- REST APIサービス
- DBとのアクセスを担うサービス
- 顔画像・顔特徴量の保存を担うサービス
- 顔特徴量抽出サービス (SDKを搭載)

顔情報登録のユースケースは、"REST APIサービス"でプロダクト側から顔画像とモデルの指定を含んだリクエストを受けて、"顔画像・顔特徴量の保存を担うサービス"へ繋ぎ、"顔画像・顔特徴量の保存を担うサービス"から"顔特徴量抽出サービス"へPub/Subで顔特徴量抽出依頼のメッセージを送ることで実現している。

顔特徴量抽出サービスに新旧SDKを両方組み込み、入力値であるモデルの指定に応じて呼び出すSDKを分岐することも考えたが、SDKは1台のマシンにつき1つしかアクティベートできず、SDK1つにつきライセンス契約が必要という制限も発覚した。
そこで、新旧バージョンのSDKをそれぞれ別のコンテナで実行し、古いモデルでの顔情報登録は旧バージョンのSDKを搭載したコンテナで、新しいモデルでの顔情報登録は新バージョンのSDKを搭載したコンテナで処理するアプローチを考えた。
この設計をプロダクト側とVPoPに提案、旧モデル廃止・新モデル移行にかかる期間を見積もっていただき、その期間ぶんだけライセンスを購入、プロダクト側でも移行計画を実施するという内容で合意した。

実装については、メッセージを新旧コンテナでどう振り分けるかで悩んだ。

1. 既存のPub/Subトピックを旧コンテナ用とし、新しく新コンテナ用のPub/Subトピックを追加。"顔画像・顔特徴量の保存を担うサービス"からはモデルの指定に応じてpublishするトピックを分岐させる
2. 既存のPub/Subトピックはそのままに、新コンテナ用のサブスクリプションを追加。サブスクリプションフィルターを活用して新旧コンテナ各々が対応するモデルでの顔特徴量抽出依頼のメッセージのみを購読する

旧コンテナはプロダクトが最新モデルの利用へ完全移行でき次第破棄する計画だったので、その際にトピックごと破棄すればいいだけ、かつ既存のインフラコンポーネントに変更を加えなくてもよい案1.を採用した。

#### 成果

認証認可基盤としては機能的な後方互換を保ったまま、SDKのアップグレードを完遂した。
2025年10月には、プロダクト側からの古いモデルの利用も完全になくなり、認証認可基盤側でも古いモデルを廃止できた。

### 資産オーナー変更API実装 (2024/04 ~ 2024/06)

#### 概要

マンション管理会社の切り替え(管理替え)ユースケースに対応するため、鍵デバイス(Object/Hole)やTreasureBoxなど資産のオーナーを一括変更するAPIを設計・実装した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 業務フロー整理・データ不整合の許容範囲の検討
- Object(+Hole)・TreasureBoxのオーナー変更APIの実装

#### 使用技術

- Go
- PostgreSQL

#### 課題

オーナー変更に伴い、鍵の解錠可否に関わる契約・取引などの関連リソースの整合性をどう保つかが課題だった。「関連リソースを全て削除して作り直してもらう」「引き継ぐ範囲をパラメータで選択できるようにする」「全て引き継いで不要な分は後から削除してもらう」という複数のシーケンス案があり、それぞれのメリット・デメリットを比較検討する必要があった。

#### 取り組み

- 契約種別(Deal・DealOffer・LendContractなど)ごとに、オーナー変更時に削除すべきか引き継ぐべきかを整理した対応表を作成し、方針をチームで合意
- Object(+Hole)およびTreasureBoxについて、旧所有者から新所有者へオーナーを付け替えるAPIを実装
- 移行前後でそれぞれ正しいオーナーがリソースを取得できるか(旧所有者は取得できなくなること・新所有者が取得できるようになること)をテスト観点として整理し検証

#### 工夫した点

「鍵が開かなくなること」を最も避けるべき不整合と定義したうえで、影響範囲や実装難易度を踏まえてObject+Hole・TreasureBoxのオーナー変更APIから段階的にリリースする方針をチームで合意し、後続のDeal・LendContract等のオーナー変更APIは別メンバーが対応する形ですみ分けた。

#### 成果

管理替え業務に必要なAPI群のうち、Object+Hole・TreasureBoxのオーナー変更APIをmainブランチにマージ・リリースした。

### OIDC SSO連携機能の実装 (2024/01 ~ 2024/03)

#### 概要

BKPアカウントをより簡単に利用できるようにするため、OIDCによるSSO連携機能(ログイン・組織サインアップ)を設計・実装した。

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- OIDC連携方式の設計・見直し
- ログイン機能・組織サインアップ機能の実装(appgate・idcontroller)・E2Eテスト作成

#### 使用技術

- Go
- OpenID Connect (OIDC)

#### 課題

当初はプラットフォーム全体で汎用的なID連携基盤を設計していたが、OpenID Providerへの登録を進める段階で、プライバシーポリシーや利用規約の扱いがサービスごとに異なり、プラットフォーム側で一元管理するには密結合になりすぎるという問題が判明し、この設計は断念することになった。あらためてスコープを絞り、ログイン・組織サインアップという具体的なユースケースに対応する形で再設計する必要があった。

#### 取り組み

- 認可コードグラントを2度行うシーケンス(サービス→BKP→OpenID Provider→BKP→サービス)としてAPIを再設計
- OIDC SSOによるログイン機能をappgate・idcontrollerに実装し、E2Eテストを作成
- OIDC SSOによる組織サインアップ機能をappgate・idcontrollerに実装し、E2Eテストを作成

#### 工夫した点

最初の設計が頓挫した反省を踏まえ、検証段階から必要な規約・ポリシー面の要件を洗い出したうえで、サービスをまたいで汎用化することを狙わず、ログイン・組織サインアップという個別のユースケース単位でAPIを設計し直すことで、実装をスコープアウトさせずに前進させた。

#### 成果

OIDC SSOによるログイン機能・組織サインアップ機能をリリースした。

### パスキー認証機能の実装 (2023/10 ~ 2023/12)

#### 概要

自社の複数プロダクトを支える認証認可基盤にパスキー認証機能を実装

#### 担当

チームメンバー:

- エンジニア: 3名

役割:

- 処理シーケンス図の作成
- プロトタイプの実装
- バックエンド・フロントエンドの実装

#### 使用技術

- Go
- JavaScript (Web Authentication API)
- Firebase Hosting

#### 課題

プロダクトへのログイン方法はID・パスワードによる認証がメインだが、パスワードはフィッシングの被害を受けやすく安全とは言えない。
UXを損ねずにパスワード以上の安全性を実現できるパスキー認証機能を認証認可基盤に実装し、プロダクトに提供することで顧客の安全で快適なサービス利用に貢献できると考えられた。

#### 取り組み

- 選定したライブラリ(go-webauthn/webauthn)を使用して、パスキー認証のUXを体験できるプロトタイプを作成し、チームに共有
- Goを使用したバックエンドの開発
- HTML/JavaScriptを使用したパスキー認証のテストページの開発。テストページはFirebase Hostingでデプロイし、Datadogのブラウザテストでパスキー登録・認証ができるかを日次で確認
- VPoP・プロダクト側のマネージャー・デザイナーをお呼びし、テストページを使って、パスキーの必要性・パスキー認証のUXについて説明するデモを実施

#### 工夫した点

認証基盤とプロダクトでドメインが異なることによりパスキー認証機能が提供できない問題に対し、リクエストから動的に RP ID を設定するアプローチを立案・実装し、解決した。

詳細はこちらのスライドで解説: https://speakerdeck.com/bitkey/challenges-and-solutions-for-passkey-authentication-implementation-in-authentication-and-authorization-infrastructure-supporting-multiple-brands-b71cf3bc-925d-441d-9c01-337f1bd26c73

　
デモにおいては、技術的な知識の非対称性に配慮して平易な言葉を選んだり、実際にパスキー登録~認証を体験してもらうことで、パスワード認証と比べてのメリットや、パスキーのUXについてイメージを持ってもらいやすくした。

#### 成果

認証認可基盤の機能としてはパスキー認証を備えるに至った。

学びとして以下のような知見を得た。

- 複数の異なるブランド(ドメイン)のプロダクトを展開する企業の認証認可基盤にパスキーを実装する際は、RP IDの管理に工夫が必要である

## 株式会社ビットキー プロダクト開発部 (2023/01 ~ 2023/09)

### bitlock Latch集合ロッカー機能 (2023/07 ~ 2023/09)

#### 概要

スマートロック製品「bitlock Latch」を用いた集合ロッカー(共有ロッカー)機能のフロントエンド開発を担当。

#### 担当

チームメンバー:

- PdM: 1名
- エンジニア: 2名

役割:

- 概要画面・マスターパスコード機能の設計・実装
- 予約設定一括編集・ユーザーグループ単位のアクセス制御機能の実装

#### 使用技術

- TypeScript
- React
- Cloud Firestore
- Cloud Run

#### 課題

bitlock Latchの「集合ロッカー」は実体のハードウェアを持たない新しい概念であり、既存の個別スマートロック(扉)向けに作られていたUIロジック・データモデルの判定条件では正しく分岐できず、QRコードが表示されない、マスターパスコード機能が未実装であるなど、複数の機能欠落が発生していた。また、複数の扉に対して予約設定を効率よく行う一括編集機能や、特定の従業員グループにのみ予約権限を絞る権限制御機能も必要とされていた。

#### 取り組み

- 概要タブのレイアウトを刷新し、QRコード表示パネル・マスターパスコード表示パネルを追加。マスターパスコードは、フロントエンドから認証認可基盤へ直接アクセスさせない設計とするため、Firestore→認証認可基盤→専用Cloud Runエンドポイントというデータ取得経路を設計し、TOTPベースで発行
- 扉一覧の一括編集UIを拡張し、予約可否・デフォルト予約公開設定・予約可能ユーザーグループを、複数の扉へ一括で適用できるように実装
- ユーザーグループ単位のアクセス制御を、一括編集・個別の扉詳細・新規登録の3つの導線すべてに実装

#### 工夫した点

マスターパスコードのカウントダウンタイマーが、ネットワーク遅延の影響でマイナス表記になってしまう不具合について、まず「取得中はカウントを止める」という緩和策を先に入れて影響を抑えつつ、本番相当環境でログを仕込んだ検証用プレビュー環境を別途用意することで、再現性の低い原因(タイマーの判定条件の誤りとカウント値の下限が未設定だったこと)を特定・修正した。
また、ユーザーグループが1件のみ設定されている場合に権限判定が誤って機能せず、本来予約できるはずのユーザーにロッカーが表示されなくなるクリティカルな不具合を発見・修正し、あわせて判定ロジックをリファクタして保守性も改善した。

#### 成果

集合ロッカー機能のオーバービュー表示・QRコード表示・マスターパスコード表示・扉一覧の一括予約設定編集・ユーザーグループ単位の予約権限制御を実装し、リリース可能な品質に仕上げた。

### スペース利用レポート機能 (2023/03 ~ 2023/04)

#### 概要

会議室・座席予約の利用実績を可視化する「スペース利用レポート」機能の集計ロジック改善とダッシュボードUIの改修を担当。

#### 担当

チームメンバー:

- PdM: 1名
- エンジニア: 3名

役割:

- 集計ロジックの不具合調査・修正
- ダッシュボードUIの改修
- 大手企業向け展示会デモ対応

#### 使用技術

- TypeScript
- React
- ApexCharts

#### 課題

予約率・実質利用率・座席利用率・外部ユーザー比率といった主要指標を可視化するダッシュボードにおいて、集計単位の不整合(時間単位と日単位の混在)や稼働時間設定の考慮漏れ、指標定義自体の誤りなど、実データでは信頼できない数値が算出されてしまう複数の重大な不具合が存在していた。加えて、大手企業向けの展示会・商談でのデモ要件にも並行して対応する必要があった。

#### 取り組み

- 予約率・外部ユーザー比率・実質利用率・座席利用率の集計ロジックの不具合を1つずつ特定し修正
- 利用者層(内部/外部ユーザーの利用状況)セクションを、集計単位・グラフ種類から見直して再実装
- 会議室/ワークスポットの切り替えタブを各コンポーネントからヘッダーへ統合するUXリファクタリングを実施
- 大手企業(オカムラ)の展示会向けに、既存コンポーネントへ影響を与えない専用コンポーネントを分離し、デモ用のUI調整とモックデータ化による描画高速化を実施

#### 工夫した点

ApexChartsで値が0のときに棒グラフが描画されないというライブラリ起因の不具合が発生した際は、GitHub Issueを追跡しつつバージョンを管理することで対応した。
展示会向けの特別要件は、既存コンポーネントに影響を与えないよう専用コンポーネントとして切り出して開発し、実データでは動作が重すぎることが判明した変更(デフォルト集計期間の変更)は本番環境への適用を見送り、モックデータのみを扱う展示会用コンポーネントに限定して適用するなど、本番影響を避けながら要件を満たす設計判断を行った。

#### 成果

予約率・外部ユーザー比率・実質利用率・座席利用率など主要指標の計算不具合を解消し、レポートが提供するデータの信頼性を担保した。あわせて実施した大手企業向けの機能改善・デモ高速化により、商談・展示会でのプロダクトデモに貢献した。

### 入退室管理SaaSの速度改善 (2023/01 ~ 2023/06)

#### 概要

自社開発toB SaaSのページの初期描画までの速度改善

#### 担当

チームメンバー:

- PdM: 1名
- エンジニア: 4名

役割:

- ボトルネックの調査
- 修正方針の設計・実装

#### 使用技術

- TypeScript
- Node.js
- React
- Cloud Firestore

#### 課題

不具合による顧客からの問い合わせが多く、チームとして新規機能の開発を止めて品質の改善を行う意思決定がなされた。

中でも温度感の高いものとして、「サイトの挙動が重い」という類のものがあった。
「ページを開くのにx秒ほどかかってしまう」というものから、顧客が利用するPCのスペックによってはブラウザがクラッシュしてしまいそもそも機能が利用できないという事象も確認されていた。

#### 取り組み

リファインメント・プランニングにおいて優先的に取り組むと決めた機能から順に、顧客がどういう操作をするときの・どの処理が遅いのかボトルネックを調査し、解消していった。

#### 工夫した点

ボトルネックの特定は、基本的に以下の手順で行った:

1. ローカル環境に状況を再現
2. React Developer Toolsで表示の遅いコンポーネントを特定
3. コンポーネント内で行っている処理に`console.time()`を追加して計測

4つの機能・画面の速度改善を担当。

- 予約履歴詳細画面
  原因は、予約履歴に付随して画面に表示しないといけないデータを取得するために ①Firestoreにクエリ ②メモリ上でfilter、という処理を行なっていたが、①のwhere条件ではあまり結果を絞り込めておらず、大量の不要なデータを毎回クエリしていたことだった。
  ②のfilterの条件と同等のwhere条件でFirestoreへのクエリを行ってから、①のwhere条件と同等のfilter処理を行うように修正し、最終的な絞り込みの条件を変えずに速度だけを改善した。Firestoreのインデックスに手を加えても良かったが、当時はFirestoreからRDBに移行しようという別プロジェクトがあったため、Firestoreに変更を加えずに実現できる方法を採用した。
- アクセスコントロール詳細画面
  アクセスコントロールとは、いつ・誰が・どのスペースに のデータを統合して保存しており、実際に入退室を制御するリソースのことである。
  原因は、アクセスコントロールのとあるパラメータにユニーク制約があり、詳細画面でアクセスコントロールの設定を編集するときにこのパラメータの重複チェックを行うため、画面描画時に大量のデータをフェッチしてメモリ上に保持していたことだった。
  重複チェックのバリデーション処理をアクセスコントロール編集APIの方に移すことで解決した。
- 従業員詳細画面
  従業員に紐づいたアクセスコントロールも表示するという仕様になっている。
  原因は、クエリするアクセスコントロールの量が多すぎること。
  取り扱えるデータ量を減らせないかとアクセスコントロールの仕様を確認したところ、スペースの利用予約を行うと都度アクセスコントロールが作成され、作成されたアクセスコントロールはその予約でしか参照されないことを理解した。BigQueryで本番データを確認したところ、アクセスコントロール515,000件のうち97%が予約起因であることが判明。「予約起因のアクセスコントロールと同等の情報は予約履歴画面で確認できるので、従業員詳細画面で取り扱うアクセスコントロールに予約起因のものは含めなくてもよいのではないか」と考え、PdMと相談したところ承諾されたので、クエリの絞り込み条件から予約起因のアクセスコントロールを除外して解決。
- スペース詳細画面
  スペースに紐づいたアクセスコントロールも表示するという仕様になっている。
  従業員詳細画面と類似の原因・対応方針となったので割愛。

いま振り返った時の反省点として、ボトルネックの特定はRUMを使うとより効率的だったかもしれないと思う。

#### 成果

修正前の初期描画速度と比較して、約75~95%の速度改善を実現:

- 予約履歴詳細画面 75%改善 (10sec → 2.5sec)
- アクセスコントロール詳細画面 90%改善(50sec → 3sec)
- 従業員詳細画面 96%改善(55sec → 2sec)
- スペース詳細画面 90% ~ 97%改善 (1min~3min → 5sec)
